[package]
name = "qudag"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Stub crate vendored for rust-lovable optional integration"

[lib]
path = "src/lib.rs"

[features]
default = []
