fn main() {
    // Placeholder: this will become the background daemon that runs
    // the hive-mind swarm (p2p + sync + updates) and exposes a local API.
    println!("noa-ui-hived: not implemented yet");
}
