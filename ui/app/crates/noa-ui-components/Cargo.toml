[package]
name = "noa-ui-components"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "NOA unified component library - DaisyUI styling with native Dioxus"

[dependencies]
dioxus = { workspace = true }

# Optional dependencies for feature-gated re-exports
# dioxus-primitives = { version = "0.1", optional = true }
# daisy-rsx = { version = "0.1", optional = true }

[features]
default = []
primitives = []
daisy = []
