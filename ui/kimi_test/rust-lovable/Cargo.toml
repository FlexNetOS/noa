[package]
name = "rust-lovable"
version = "0.1.0"
edition = "2021"

[dependencies]
# Core Dioxus dependencies
dioxus = { version = "0.6", features = ["desktop", "mobile", "web", "fullstack"] }
dioxus-logger = "0.6"
dioxus-router = "0.6"
dioxus-hooks = "0.6"
dioxus-signals = "0.6"

# Web and HTTP
tokio = { version = "1.0", features = ["full"] }
reqwest = { version = "0.12", features = ["json", "multipart"] }
axum = "0.7"
tower = "0.4"
tower-http = { version = "0.5", features = ["cors", "fs"] }
hyper = "1.0"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = { version = "1.0", features = ["v4", "serde"] }

# Error handling
anyhow = "1.0"
thiserror = "2.0"

# Logging and tracing
tracing = "0.1"
tracing-subscriber = "0.3"

# AI and ML integrations
# Future integrations (feature flags)
ruv-fann = { version = "0.1", optional = true }
ruvector = { version = "0.1", optional = true }
qudag = { version = "0.1", optional = true }
libp2p = { version = "0.55", optional = true }

# Database and storage
sqlx = { version = "0.8", features = ["runtime-tokio-native-tls", "sqlite", "postgres", "chrono", "uuid"], optional = true }
sea-orm = { version = "1.0", features = ["sqlx-sqlite", "runtime-tokio-native-tls", "macros"], optional = true }

# Utilities
chrono = { version = "0.4", features = ["serde"] }
regex = "1.0"
once_cell = "1.20"
dashmap = "6.0"

# File and path handling
camino = "1.1"
walkdir = "2.0"

# Configuration
config = "0.14"
dirs = "5.0"

# CLI support
clap = { version = "4.5", features = ["derive"] }

# Web scraping (for Open-Lovable functionality)
scraper = "0.20"
select = "0.6"

# Code generation and parsing
syn = { version = "2.0", features = ["full"] }
quote = "1.0"
proc-macro2 = "1.0"

[features]
default = ["web", "desktop"]
web = ["dioxus/web"]
desktop = ["dioxus/desktop"]
mobile = ["dioxus/mobile"]
fullstack = ["dioxus/fullstack"]
ai-integrations = ["ruv-fann", "ruvector", "qudag", "libp2p"]
database = ["sqlx", "sea-orm"]

[[bin]]
name = "rust-lovable"
path = "src/main.rs"

[dev-dependencies]
tokio-test = "0.4"